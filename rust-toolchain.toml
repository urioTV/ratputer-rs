[toolchain]
# The Nix devshell overrides this channel with its pinned Xtensa toolchain path.
channel = "esp"
profile = "minimal"
components = ["rust-src", "rustfmt"]
targets = ["xtensa-esp32s3-none-elf"]  # esp fork: the triple omits "unknown"
